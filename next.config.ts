import type { NextConfig } from "next";

/**
 * Configuración de Next.js.
 * Mínima a propósito: el esqueleto no necesita nada especial todavía.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
